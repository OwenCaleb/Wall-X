sudo apt-get install -y rsync

rsync -avP \
  /mnt/data/liwenbo_datas/lerobot/aloha_mobile_cabinet/ \
  mindon:/mnt/nas_ssd/data/wallx/data/lerobot/

不要在目标文件夹下创建！是自带的传送文件夹

下面这个链接也是

ln -sfn /mnt/nas_ssd/data/wallx/data/lerobot/aloha_mobile_cabinet /mnt/nas_ssd/workspace/wenboli/projects/Wall-X/Datasets/lerobot

python - <<'PY'
import glob
from datasets import load_dataset

files = sorted(glob.glob("/mnt/nas_ssd/data/wallx/data/g1/lerobot/Teleop_251103_sort_Anonymous_10Hz_subtask/data/chunk-000/episode_000000.parquet", recursive=True))
print("num parquet:", len(files))
ds = load_dataset("parquet", data_files=files, split="train", keep_in_memory=True)
print("OK rows:", len(ds))
print("columns:", ds.column_names[:20])
PY